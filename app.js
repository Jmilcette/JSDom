const bookList = document.querySelector('#book-list');
//using .parentNode grabs the parent or the node that the selected node is under
console.log('the parent node is:', bookList.parentNode);
//using .parentElement basically does the same thing as .parentNode
console.log('the parent node is:', bookList.parentNode);
